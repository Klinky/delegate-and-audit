interface:
  display_name: "Delegate and Audit"
  short_description: "Own delegation through monitoring and audit."
  default_prompt: "Use $delegate-and-audit to delegate bounded work, keep every helper reconciled, and audit results before accepting them."
